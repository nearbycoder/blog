---
import { site } from "../data/site";
---

<ol class="timeline">
  {
    site.work.map((item) => (
      <li>
        <>
          <span class="label">{item.range}</span>
          <h3>{item.company}</h3>
          <p>{item.role}</p>
        </>
      </li>
    ))
  }
</ol>
