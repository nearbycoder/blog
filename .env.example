# Enable only after the Resend domain and subscription flow are verified.
NEWSLETTER_ENABLED=false
NEWSLETTER_FROM=Josh Hamilton <newsletter@nearbycoder.com>
# Sensitive server-only Vercel environment variables. Never prefix with PUBLIC_.
RESEND_API_KEY=
NEWSLETTER_TOKEN_SECRET=
CRON_SECRET=
# Use a separate test segment/topic and token secret for preview deployments.
RESEND_SEGMENT_ID=
RESEND_TOPIC_ID=
